# حالة البناء والاختبار

## ناجح

تم التحقق من سلامة بناء Python نحوياً، وتشغيل اختبارات النواة الخاصة بإنشاء الجذاذة، قالب المراحل، النص العربي، حفظ JSON، وإنشاء DOCX حقيقي قابل للفتح كحزمة Office. كما تم فحص بنية DOCX ووجود عنصر جدول Word وحدوده وخصائص RTL داخله. واختُبرت دورة البيانات الكاملة: إنشاء، تعديل، حفظ، إعادة فتح، فرضيات، نسخ، حذف، ونسخة احتياطية.

تم تضمين واجهة عربية RTL، معلومات أساسية، إدارة الجذاذات والقوالب، البحث، النسخ والحذف، المراحل، الأنشطة، الأسئلة، السندات، الفرضيات، تمحيص الفرضيات، القيم، التقويم، التعلم الذاتي، المعاينة الجدولية، PDF، Word، الطباعة عبر HTML، والنسخ الاحتياطي والاسترجاع. أُنشئت عينة عربية طويلة من سبع صفحات A4 للتحقق من امتداد PDF وتكرار رأس الجدول.

## قيد معروف

المطابقة البصرية مع النموذج المرجعي أساسية في الأعمدة الخمسة واتجاه RTL والحدود، لكنها ليست نسخة طبق الأصل من كل تفاصيل الترويسة ودمج الخلايا الأصلي؛ يلزم تحسين إضافي إذا كان المطلوب استنساخ النموذج حرفياً.

بيئة البناء الحالية Linux ولا تحتوي على Windows أو Wine أو MinGW أو Inno Setup؛ لذلك لم يتم إنتاج أو اختبار EXE Windows فعلياً. أضيف Workflow مستقل في `.github/workflows/windows-build.yml` يعمل على Windows Runner عند تشغيله من مستودع GitHub، ويقوم بالاختبارات وبناء EXE وPortable وSetup واختبار بدء التطبيق المثبت. تم فحص `BUILD-WINDOWS.bat` وأصبح ASCII-only، لا يستدعي Python التفاعلي، وينشئ venv ويستخدم `python -m PyInstaller` ويجهز Portable وSetup عند توفر Inno Setup. تم إنتاج نسخة Linux اختبارية فقط للتحقق من قابلية التغليف.

## طريقة الوصول إلى Windows النهائي

على Windows، ينفذ المطور أو المستخدم الذي يملك بيئة البناء ملف `build/build_windows.bat` مرة واحدة، ثم يستخدم Inno Setup مع ملف `build/Tarbawi-Jodada.iss`. بعد ذلك يعمل الناتج المستقل دون Python أو Node.js أو npm أو خادم أو إنترنت.
